# 蘑菇.io — 设计文档（GDD-lite）

> 概述：蘑菇战争（Mushroom Wars）核心循环 + 抢地盘.io 形态的单人轻量网页游戏。
> 状态：v0 原型设计，数值未经 playtest，全部标 `[PLACEHOLDER]`。
> 读者：接手实现的开发者 / 黎总做方向判断。
> 关联：`index.html`（同目录可跑原型）。

---

## 1. Fun Hypothesis（一句话）

**这游戏好玩的核心是——用手指划出菌毯，看着自己的蘑菇领地像活物一样吞掉对面，3 分钟占满整座岛就爽。**

写不清楚就不往下走。这句话成立的前提：占领动作必须"一指完成"、占领结果必须"肉眼可见地变大"（翻色 + 出兵球实体 + 碰撞/命中火花）。

---

## 2. Design Pillars（不可妥协的体验标准）

| # | 支柱 | 落地含义 |
|---|------|----------|
| P1 | 一指操作 | 点己方节点 → 拖向目标 → 松手派兵。零学习成本，源码层 = 1 次 touch + 1 次 drag。 |
| P2 | 兵力即唯一资源 | 所有决策围绕"兵往哪送"。不引入金币/木材等二级资源，保证"轻"。 |
| P3 | 短局必有爽点 | 每局 3-5 分钟内至少有一次"吃掉一块地 / 顶住反扑"的正反馈。 |
| P4 | 解锁钩子 | 用"新蘑菇种类 / 新地图 / 新兵种建筑"驱动每天回流，而非数值付费（暂不变现）。 |

---

## 3. 核心循环（三层钩子）

- **Moment-to-moment（0-30s）**：选节点 → 拖兵 → 看到兵球逐颗喷出（建筑数字逐颗 -1）→ 路中与敌球对撞火花 → 目标节点翻色。
- **Session（3-5min）**：从 1 个节点扩张到碾压全场，期间不断"看到自己的颜色在地图上蔓延"。
- **Long-term（hours-weeks）**：解锁蘑菇物种（不同产能/容量曲线）+ 新兵种建筑（箭塔/弓箭手兵营）+ 每日地图挑战，纯留存向。

---

## 4. 经济模型：Sources & Sinks

兵力 = 唯一货币。**驻军可暂时超容量**（进驻兵无上限累加），但自然增长封顶于容量——上限是"sink"，杜绝无限通胀。

```
       产兵(每节点按容量比率,封顶)             派兵(逐颗扣数, pending 防超发)
  [节点] ──────────────▶ [兵力池] ──────────────▶ [占领/战斗]
     ▲                        │
     │       容量上限 = 自然增长 Sink；进驻兵可超上限
     └──── 占领新节点 → 更多产兵点
```

- **Source**：你拥有的每个节点按 `容量 × 增长率` 持续产兵，**自然增长封顶于该节点容量**。
- **Sink**：派兵时兵力逐颗离开节点（`pending` 承诺数防连点超发）；节点被夺走则失去该产兵点。
- **超容量驻军**：进驻兵（球到达并入）无上限累加，但不会触发自然增长——"能囤但不会自己涨"。
- **通胀防护**：容量上限 = 天然 sink。兵力不会无限堆积，逼玩家持续扩张。
- **风险点**：若增长率过高 + 地图过小，会出现"谁先手谁碾压"的雪球。需用 playtest 调 `增长率` 与 `AI aggressiveness`。

---

## 5. 数值设计（全部 `[PLACEHOLDER]`，附假设与验证路径）

| 变量 | 值 | rationale | 验证路径 |
|------|----|-----------|----------|
| 节点基础增长率 | 容量 × 5% / 秒 | 30 容量节点 ≈ 1.5 兵/秒，单局节奏不至于太慢 | playtest 测单局时长是否落在 3-5 min |
| 单节点容量 | 村庄 10/30/50；**箭塔固定 10** | 容量即战略价值；箭塔驻军上限 10（塔兵自然回复上限） | 观察玩家是否优先抢大点/抢塔 |
| 派兵规则 | 出兵后本村留 1 兵(keep=1)；**逐颗扣数**：send 只记 `pending` 承诺数，每颗球真正出发时 `pop--`（建筑数字逐颗 -1，防连点超发）；兵力全程整数 | 用户硬规则"出到只剩一个兵"；增长用累积器满 1 才 +1 | 与雪球速度联动，需 playtest |
| 球飞行速度 | 基准 120 px/s × `fleetSpeedMul`(默认 0.8) | 兵球沿路径飞行；**每颗球 = 1 个实体兵（有 hp/atk/攻频/碰撞半径/射程）**，emitDur=0.6s 错峰出村 | 实测手感 |
| 出球间隔 `emitDur` | **0.6s**（可调大） | 同一队球"陆续出村"的发射时长；越大出球间隔越宽、球流越松散 | 手感/节奏 |
| 球行走速度倍率 `fleetSpeedMul` | **0.8**（可配置） | 叠加在 `fleetSpeed` 上的整体倍率：1=原速、0.8=八成速、0.5=减半；改这一个值即可统一调快/调慢 | 手感/节奏 |
| 球碰撞/分离半径 `ballSepR` | **7 px**（可配置） | 每颗球的碰撞范围；两球中心最小间距 = 2×此值，过近则互相挤开 | 视觉/物理感 |
| 球分离推力 `ballPush` | **40 px/s**（可配置） | 球间分离的推力强度，越大挤得越开 | 视觉/物理感 |
| 球回路径弹性 `ballSpring` | **0.2**（可配置） | 被挤开后弹回路径锚点的速度，越大越快归位 | 视觉/物理感 |
| 兵种数值 | 见下表 `CFG.units` | 步兵/弓手/箭塔 差异化 | playtest 平衡 |
| 弹道速度 | 340 px/s（`CFG.shotSpeed`） | 箭矢肉眼可见飞行，命中结算伤害 | 手感/命中反馈 |
| 地图规模 | 1 玩家 + 2 AI，**有机岛屿 + Voronoi 不规则分块领地**，约 11–16 节点，含 **2 中立箭塔 + 2 中立弓箭手兵营**（随机落在非基地中立格） | 岛屿外形带噪声；泊松采样+远离岛边取地块中心；垂直平分线裁剪生成 Voronoi 单元 + Chaikin 平滑边界；**建筑画在地块质心**，渲染用向质心内缩 6% 的副本（物理上杜绝格子重叠并留块间缝隙）；采样不足 3 点时确定性三角形兜底，永不崩溃；**箭塔/兵营 = 中立建筑，派兵进驻=占领，别家可夺走** | playtest |
| 胜利条件 | **全场除中立外仅剩一个势力**（删除 60% 占比） | 明确终点，避免无限局 | 测平均结束时间 |
| 单局目标时长 | 3-5 min | 用户给定约束 | 实测分布 |

### 兵种数值表（`CFG.units`）

| 兵种 | 来源建筑 | 攻击力 | 血量 | 攻频 | 碰撞半径 | 射程 | 行为 |
|------|---------|-------|------|------|---------|------|------|
| 步兵 melee | 普通村庄 / 箭塔 | 10 | 30 | 0.5s | 5px | 0 | 近战：与敌球在行进路上碰撞互打，直到一方消失或离开碰撞范围 |
| 弓箭手 archer | 弓箭手兵营 | 5 | 20 | 1s | 5px | 190px | 飞行中自动停下，站桩远程攻击射程内敌方行进中的球（生成可见弹道） |
| 箭塔 tower | 中立箭塔（建筑） | 10 | — | 1s | 12px | 230px | 被占领后自动远程攻击射程内敌方行进中的球；塔兵自然回复上限 10 |

> 所有数值均为假设，跑通 fun hypothesis 后必须以 playtest 重画。若实测单局是 8 min，整条进阶/AI 节奏要重画。

### 阵营系统（开局可选，v0.3 新增）

开局先弹"选阵营"遮罩，玩家从 4 个阵营里挑 1 个；红、黄两家 AI 从**剩余 3 个里随机抽 2 个**（每局不同，增加重玩性）。阵营差异全部用**乘数**实现（挂在 `CFG.factions`，出兵瞬间套用），且**成对取舍**——没有任何阵营在所有维度都占优（注意：本游戏无经济系统，维度只有生产/移动/单兵/防御 4 项）。

| 阵营 | 人设 | 生产(emitMul) | 移动(speedMul) | 单兵(hp·atk) | 防御(towerMul) | 性格 |
|------|------|------|------|------|------|------|
| 河南 | 中原粮仓 | 0.7（出球快 43%） | 1.0 | hp0.85（脆） | 1.0 | 人海压制 |
| 上海 | 魔都精工 | 1.2（出球慢 20%） | 1.0 | hp1.18·atk1.12（强） | 1.0 | 少而精 |
| 北京 | 帝都铁壁 | 1.0 | 0.9（慢） | 1.0 | 1.4（塔硬） | 龟缩反击 |
| 广东 | 岭南突击 | 0.9（略快） | 1.25（快） | hp0.9（略脆） | 0.85（薄） | 闪电截击 |

**乘数落地位置（代码为准）：**
- 出兵速度：`send()` 里 `travel = dist/(fleetSpeed·fleetSpeedMul·speedMul)`
- 出球间隔：`delay = (i/cnt)·emitDur·emitMul`
- 单兵属性：球 `hp = units.hp·hpMul`、`atk = units.atk·atkMul`（血条用 `hpMax`）
- 防御：塔 `atk·towerMul`、`range·towerMul`（射程圈绘制同步乘 `towerMul`）
- `facMul(owner)` 取某势力的乘数；`factionOf{1:玩家,2:AI,3:AI}` 记录本局分配
- 选阵营遮罩 `#factionSelect` 由 `buildFactionCards()` 从 `CFG.factions` 动态生成卡片（四维强弱 ▲/▼/— 实时算自乘数），改乘数即改卡片，无需动 UI

---

## 6. 系统交互矩阵（提前暴露打架风险）

| 系统 A | 系统 B | 关系 | 备注 |
|--------|--------|------|------|
| 派兵 | 容量上限 | intended | 满了逼你出兵，是正循环；进驻可超上限但不自然增长 |
| 球-球空中对撞 | 派兵路线 | intended | 不同势力球行进中相遇即**贴身挡下、原地持续互打**（碰撞火花+白闪），**不穿过对方**；弓手/箭塔为射程内远程持续打 |
| 箭塔 | 行进中的兵 | intended | 被占领后自动远程攻击射程内敌球，形成"路过就被削"的威胁带 |
| 弓箭手兵营 | 行进中的兵 | intended | 弓手飞行中停下站桩输出，克制敌方兵线但自身脆弱（血20） |
| 箭塔/兵营 | 中立抢占 | intended | 开局中立，先到先得；被夺走会失去该能力 |
| AI 派兵 | 玩家反扑 | intended | AI 抢玩家节点制造张力 |
| 解锁物种 | 平衡 | acceptable→需监控 | 新物种若改产能曲线须重测通胀 |
| 触控 | 桌面鼠标 | intended | 同一套 pointer 事件覆盖 |

---

## 7. Playtest 失败信号（先定义"坏掉长什么样"）

- **A 失败**：单局 > 6 min 仍未分胜负 → 增长率/容量需调。
- **B 失败**：玩家 30 秒内不知"该点哪" → 引导/视觉反馈缺失。
- **C 失败**：AI 完全不进攻或瞬间碾压 → aggressiveness 参数失调。
- **D 失败**：占领无"爽感"（翻色不明显）→ 反馈通道（视觉）问题，先查美术再查机制。
- **E 失败（新增）**：远程攻击看不到/看不出在打谁 → 弹道、命中火花、锁定线缺失，先查视觉再查数值。
- **F 失败（新增）**：玩家看不懂弓手为什么停下 → 锁定线/锁定环/准星未画或过淡。

---

## 8. 美术基线

简约矢量线条、浅色主题：背景 `#f5f2ea`，沙色岛屿 `#f0e6da`；**玩家=绿 `#3fb54a`，AI 红=`#e8654f`，AI 黄=`#e0a02f`**（阵营名 河南/上海/北京/广东 随开局选择绑定到这三色），中立=灰 `#9a9a8e`。地图=**有机岛屿 + Voronoi 不规则分块领地**（岛屿外形带噪声，地块边界垂直平分线裁剪 + Chaikin 平滑），每块地中心一个**建筑图标 + 兵力药丸徽章**（随地块大小缩放、锚定在地块质心 → 永远居中、不溢出、不重叠）：

| 建筑类型 | 造型 | 说明 |
|---------|------|------|
| 村庄 village | 蘑菇（圆伞+菌柄+伞点+玩家高光弧） | 出兵=步兵 |
| 箭塔 tower | 高塔（塔基+梯形塔身+尖顶+窗口）；**被占领时画淡色射程圈** | 出兵=步兵；自身远程射击 |
| 弓箭手兵营 archery | 蘑菇+伞面弓徽章（弯弓+弦+箭） | 出兵=弓箭手 |

**球实体**（每颗球=1个兵）：势力色圆球+白色高光+血量条（掉血才显示）；弓箭手带小三角箭头（朝目标方向）。**碰撞反馈**：近战对撞瞬间双方白闪 + 碰撞点迸溅火花粒子。**远程弹道**：箭矢（带方向头）从射手飞向目标，命中时火花+白闪——远程攻击肉眼可见。**弓手锁定**：弓手停下射击时，画虚线瞄准线连到目标 + 目标锁定环（呼吸动画）+ 十字准星——"为什么停下"一眼看懂。出兵时球逐颗从村口喷出（emitDur=0.4s），**建筑数字逐颗 -1**；地块被攻击时外圈闪一下。

---

## 9. 黎总浏览器自测指引（无需装任何东西）

1. 双击 `index.html`（或拖进浏览器），应直接开局，无需联网。
2. 屏幕是一块有机形状的大岛，内部被曲线分成不规则地块；每块地**中心**有一个建筑：**绿色蘑菇=你（河南）**，**红=上海**，**黄=北京**，**灰=中立（待抢）**；部分中立格是**高塔（箭塔）**或**带弓徽章的蘑菇（弓箭手兵营）**。图标下方白色药丸徽章=当前兵力（永远是整数），地块填充色=所属势力（中立保持沙色），相邻地块之间有清晰缝隙、**互不重叠**。
3. 操作：**鼠标按住你的绿色节点 → 拖向任意节点 → 松手**，即把村里兵几乎全派出、只留 1 个；**AI 同样只留 1 个**。出兵时你会看到**和出兵数一样多**的球一颗颗从村口喷出、**建筑数字逐颗 -1**，排队飞向目标（几颗球 = 几个兵，一一对应）。
4. **兵种与战斗**：普通建筑出步兵（绿/红/黄球）；弓箭手兵营出弓手（球带箭头）；箭塔和弓手会**射出可见箭矢**远程攻击射程内的敌方行进球。不同势力的球在**行进路上相撞**会互打（白闪+火花），直到一方消失或离开碰撞范围。
5. **箭塔/兵营抢占**：开局灰塔和灰弓兵营是中立，谁先派兵进驻归谁；**别家也能派兵夺走**。箭塔被占领后自动射击，弓手从兵营出发后会自动停下远程攻击射程内敌球（身上会画锁定线）。
6. 每颗球飞到目标节点就结算 1 个兵：是友军就并进去（**超过上限也继续累加**）、是敌军就和它**成对消融**（节点外围闪一下=打架），敌兵抵消光后节点翻成你的颜色 = 占领成功。
7. 顶部三条彩色占比条是你的控制占比（绿=河南 / 红=上海 / 黄=北京）；**全场除中立外只剩一个势力即胜**（不再是 60%）。

**自测时重点看这 6 个失败信号（对应第 7 节）：**
- **卡死**：玩了 5 分钟地图纹丝不动、谁都抢不动 → 是早期"僵局 bug"，若复现说明还要调中立初始兵力。
- **太快/太慢**：一局 30 秒就结束（被人滚雪球碾压）→ 增长速率 `growthRate` 偏高，需调低；拖到 8 分钟不分胜负 → 需调高或检查 AI 出兵门槛。
- **AI 不动/瞬间秒杀**：AI 完全不抢，或开局 10 秒就灭了你 → `aiTick` / 出兵门槛失调。
- **爽感缺失**：占领时没有明显"翻色/变大"反馈 → 先查美术再查机制。
- **远程看不到（新增）**：箭塔/弓手攻击时看不到箭矢、命中没火花 → 弹道/粒子渲染问题，先查 `drawShots/drawFx` 再查数值。
- **弓手白停（新增）**：弓手停下但没画锁定线/锁定环 → 锁定视觉缺失，先查 `drawBall` 的锁定分支。
- **兵力变小数**：看到节点数字出现小数（如 12.3）→ 整数化回退，立即报 bug（当前用累积器满 1 才 +1，不应出现）。

> 所有数值在 `index.html` 顶部 `CFG` 对象里，改完刷新即可见效；但务必记下改动，便于后续回滚。

## 界面与流程

- 开局流程：加载 → **开始游戏标题界面**（`#startScreen`，点「开始游戏」）→ 选阵营面板（`#factionSelect`）→ 选完开战。
- 游戏内：**暂停按钮**在顶栏「重开」右侧；点「暂停」置 `paused` 冻结模拟（主循环跳过 `update` 且冻结计时 `gameTime`），弹出「已暂停」遮罩，点「继续」恢复。恢复时重置 `lastTs` 避免 dt 跳变。
- 计时改用累计游戏内时间 `gameTime`（仅未暂停时增长），不再依赖 `performance.now()-startTime`。
