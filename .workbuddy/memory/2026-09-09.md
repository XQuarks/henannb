# 2026-09-09

## P0 开工（黎总已提交 git，授权动代码；改前已备份 index.html 到桌面 index.html.bak-20260908-preP0）

### 已完成 3 项（每项都跑测试验证）

**① 0.4 删 L1 重复 outro 键**（index.html 原 2805/2817 两处内容相同，后者覆盖前者）
- 删掉 midCine 之前那条，保留之后那条。`grep -c "最后一面绿旗插上了被烧的麦田"` = 1 ✅
- 验证：e2e-flow ALL OK

**② 0.2 修 e2e-terrain 历史红灯「缩放锚点不动」**（terrain 96/1 → **98/0**）
- **根因不是我最初猜的 G_TOP**！用探针 tests/_probe_zoom.js（head -54 e2e-terrain.js 拼自定义 PROBE，可复用到任何内部状态探测）实测：
  - y 方向 dy=0（没问题）
  - x 方向 dx=-59：L1 全景 zoom 0.77、地图+边距 bw=863px < 屏幕 1280px，**放大后视野比地图还宽 → clampCam 走「vw>=bw 强制居中」分支 → 锚点必然漂移**。这是产品设计（小图放大居中），不是 bug。
  - 且 L1 可平移范围仅 ~35px，任何偏离中心的锚点都会撞边界钳制。
- **顺带修的真问题**：`clampCam()` 垂直方向用 `vh=H/zoom` 且直接钳 cam.y，与 `camHome` 的 `-(G_TOP/2)/z` 补偿**口径不一致** → 已改为按可视区 `vh=(H-G_TOP)/zoom` + `off=(G_TOP/2)/zoom` 折算（cy=cam.y+off 钳制后 cam.y=cy-off）。修掉潜在的"缩放时画面跳一下"。
- **测试改为分场景断言**：
  1. 换随机大图 `buildMap('M0',{seed:7})`（27 格，地图>视野）→ factor 2.5 → dx=0.00/dy=0.00 ✅ 断言锚点不动
  2. 回 L1 小图 factor 1.5（视野>地图）→ 断言应居中
- 全套复验：v5 61/0 · flow OK · camp 21/0 · director 140/0 · m4 36/0 · m3 5/0 · fixed-bridge 91/0 · boss 21/0 · difficulty 16/0 · terrain 98/0 · check-all-levels 40/40

**③ 0.5 修 check-geom 快照依赖 + 产物污染根目录**
- `gen-final-map.js`：加 OUT_DIR=tests/_out（自动 mkdir），快照→`_out/_snaps.json`，预览→`_out/map-preview.html`
- `check-geom.js`：快照路径改 `_out/_snaps.json`，缺失时 **execSync 自动跑 gen-final-map.js**，不再抛错
- 新增 `.gitignore` 忽略 `tests/_out/`
- 验证：删掉旧快照后直接跑 check-geom → 自动生成并通过；根目录 git status 干净

### 下一步判断（重要）
**0.6「L2 改名三门渡口」建议并入 H-A 一起做**，不要单独先改：
改名要动 brief + 开场对白 + 导演简报 3 段（都会说"三处渡口/夺堡开门"），若地图与 H-2 门控机制还没落地，就是**台词承诺空头**（本项目踩过的老坑）。正确顺序：**先做 H-A 的地图与机制 → 改名与台词一次性改到位**。

## H-A · L2 三门渡口（17:09 后，黎总「江的走向你自己定，方便手机端+有缩放即可」）

### 决策
沿用现有**竖直江（col3，左右分岸）**改动最小；手机端靠已落地的双指缩放+平移兜底，不为其重做布局。
江是南北走向 → 三处渡口命名改为 **北津浅滩(b) / 白石桥(B) / 南岭栈道(b)**（原方案按横向江叫「西津/东岭」，已废）。

### 重大发现：H-2 门控机制**早已存在**，不必新写
`findPath` 第 2163 行：`if(v.gate && v.owner!==owner && v!==to) continue;` → 「不是自己的堡垒不许穿行，但可派兵去打它」。
字符 `B` 本来就带 `{bunker:true, gate:true}`。所以 H-A 的核心只是**把一桥改成三渡口**，不是实现新系统。

### 改动清单
1. 新增字符 **`b` = 小关隘堡垒**（cap18/pop8，同样 gate）—— 让三座门有软硬之分（中间 B=cap25/pop14 最硬）。
2. `onBridge` 标记补 `b`（原来只认 `w/e/B`，漏了小写门）→ 桥面格数才对得上。
3. `dusk` 网格替换为三渡口（7×7，江 col3，渡口在 row1/3/5）：
```
'...~...'  '.vwbev.'  '.vv~vv.'  '.PwBe2.'  '.vv~vv.'  '.Nwbe3.'  '...~...'
```
L2 由 16 格 → **23 格**，40 关体检全通过。
4. **改名「三门渡口」+ 全套台词**（brief/开场对白/导演简报 3 段/SKEL info/GUIDES 台词/outro/win）—— 因地图已就位，不再是空头承诺。天龙 L2「焚断桥」的白石桥台词**未动**（另一条线）。

### 实测验证（探针）
- 三座 gate pop=8/14/8，全中立时玩家 **0/2** 可达对岸；占下任意一座软门 → 立刻 2/2 可达；门在玩家手里 → AI 2/2 被挡（关门战术成立）。
- **AI 会抢门**：拦截 `send()` 统计，首次派兵打关隘 **t=16.7s**（grace 15s 一过就动手），60s 内派兵 4 次其中 2 次打门。
- ⚠️ **踩坑**：无头环境里兵球**全停在出发格不移动**（L1/L2/L3 都是 58/116 个球静止）——用改前备份对照结果一致，**是环境假象不是 bug**。想验证 AI 意图必须**拦截 send() 看目标**，不能看球位移（我曾据此误判"AI 瘫痪"）。

### 测试断言更新（e2e-fixed-bridge 87/4 → 92/0）
旧断言按「单桥」写死，四族 L2 循环：
- 桥面格数 `===3` → 改为 `=== gate 数 × 3`
- 「你 2 步到碉堡」→ 改为「够得到某座关隘 ≤3 步」
- 「摘掉碉堡断两岸」→ 新增 `reachWithoutAll()`，改为「摘掉**全部**关隘断两岸」（三门关摘一座还有两座）
- 竖屏段同步
- **新增 E2 段**：固化「AI 抢门」关键验收（≤45s 内派兵打门），否则以后改 AI 会悄悄失效。

## H-A 续：8s 易主冷却 + 门开关敌方喊话（14:20 后）

### 黎总新指示（待办）
**L2 三门渡口地图太四四方方，后面要再优化（有机化）**。当前网格是规整 7×7 三行渡口，先记账，暂不动。

### 8 秒易主冷却（CFG.gateLock=8）
`arriveBall` 里 pop<=0 分支最前面插入：若 `node.gate && node.gateLockUntil > gameTime` → pop 吊回 1、不掉旗、飘字「铁闸未稳…」（3s 内不重复提示）；易主成功后 `node.gateLockUntil = gameTime + CFG.gateLock`。
实测：玩家夺门→锁定至 t=8；锁定期内敌方猛攻 200 次 owner 仍 1、pop 吊在 1；过期后可正常易主。
已固化为 e2e-fixed-bridge **E3 段 3 条断言**（92 → 95/0）。

### 又一个"已存在"的机制：敌方指挥官喊话 `foeSay()`
`FOE_LINES`（4749）四族各有 taunt/press/falter/down 四类，`foeSay(kind,owner)` 4832 行用 `acQueue.push({text, npc:{name,title,face}, accent})` → **与我提案的 AI-F 完全同一套机制（含头像+族色）**，士气高涨/低落（press/falter）**其实早就实现**了。
所以 AI-F 只需补类别 + 调用点 + 限流，不必新造 UI。

### 本次新增
1. 四族各加 `gateWin` / `gateLose` 台词（门开/门关敌方开口，贴合各族语气）
2. 关隘易主处调用：`owner===1 ? foeSayAny('gateLose') : foeSay('gateWin', owner)`
3. 统一限流：`let foeSayAt=-99` + `CFG.foeBarkGap=5`（覆灭遗言 `down` 不受限）
实测：玩家夺门 → 天龙开口「门……被蝼蚁夺走了？可笑。」；1s 后再触发被冷却挡住 ✓

### 探针踩坑
验证"台词是否触发"不能看 `acQueue.length`（acPump 会立即 shift 消费掉），要看 **`acLastLine`**。

## H-1 焦土复原（人类 L1，14:35）

### 设计
`c` = 焦土村（被哥布林烧坏的村）：**cap12 / pop4 / growMul 0.5**（烧坏 → 产兵减半、容量小）。
玩家夺回 → 20s 倒计时 → **火灭地复**：charred=false、growMul=1、cap 回到 20、飘字「焦土复苏 · 产能恢复」+ 金环 + 副官台词。
**中途丢掉会中断**（charRestoreAt 归零），不是白送。教学价值：新手第一眼看到「占领 = 把地救回来」，比加数字更有说服力。

### 改动
1. `CFG` 加 `charRestore:20 / charCapBack:20 / charGrowMul:0.5`
2. 字符表加 `c`（type village cap12 pop4）+ 标记 `{charred:true, growMul:CFG.charGrowMul}`
3. `arriveBall` 易主处：玩家占焦土村 → 设 `charRestoreAt`，飘字「火灭地复…」
4. 新增 `charRestoreTick()`（独立函数便于 e2e），挂在 update 里 `barnRotTick()` 之后
5. 渲染 §3.21：焦黑 + 余烬，**透明度随复原进度递减**（能看着焦土一点点褪掉），复写自 §3.2 焚桥焦土画法
6. L1 网格 h1 改 3 格为 `c`（`'..vcv..' / '.vNTc..' / '.vvcv..'`）
7. `AIDE_LINES.human.charRestore` 加台词

### 验证
探针 5 项全过：中立不复原 / 夺回后倒计时 20s / 未到时不复原 / 到时 charred=false+cap20+growMul1 / 中途丢失中断。
已固化为 **e2e-terrain J 段 7 条断言**（98 → **105/0**）。

## D-8 · L2 三门渡口地图有机化（17:00，黎总「地图太四四方方」）

### 改法（铁律：只动陆地外形，不动渡口与江）
`dusk` 网格新值（7×7，col3 仍是江，row1/3/5 仍是 b/B/b 三处渡口）：
```
'...~...'   ← 江源窄
'.vwbevv'   ← 北津（右岸多凸一格）
'vvv~vv.'   ← 左岸向西凸出（col0 有地）
'.PwBe2.'   ← 白石桥（主门）
'.vv~vvv'   ← 右岸向东凸出（col6 有地）
'.Nwbe3.'   ← 南岭
'..~~~..'   ← 江尾三格喇叭口入海
```
两岸凸出**错开**→不对称；原来两岸都是等宽直筒，所以像格子本。

### 结果
- 23 → **26 格**，面积倍差 7.1x→7.3x，形状指标 0.516→0.525（更有机）
- 铁律未破：三座门全中立 → 玩家 **0/2** 可达对岸；三门全占 → **2/2** 可达
- 40 关体检通过（人类 L2 26 格 连通=是 我方1 敌方2）
- 全套全绿：fixed-bridge 95 / terrain 105 / v5 61 / flow OK / camp 21 / director 140 + 几何自检

### 经验
固定图有机化**不必动生成器**：`handShaped`（grid 含 '.'）会跳过 `carveFixedCoast`，轮廓完全由手写网格决定 → 改 `.` 的分布就能改外形。V11 的加权 Voronoi / 共享边细分 / 碎礁是全局生效的，新手写网格自动享受。

### 复用技巧
`head -54 tests/e2e-terrain.js > tests/_probe_x.js` + 自定义 PROBE 字符串尾部 `eval(js+PROBE)`，可在无头环境打印任意内部状态（cam/MAPBB/nodes/…）。探针用完即删。
