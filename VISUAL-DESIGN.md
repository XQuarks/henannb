# 蘑菇.io — 视觉优化设计文档（Visual Pass v1）

> 概述：在不动任何玩法/数值的前提下，对 `index.html` 做一轮整体画面升级——界面层（HUD/遮罩/开始界面）+ 画布层（背景/岛屿/兵球/反馈）共 14 项。
> 状态：已落地，JS 语法校验通过（node --check）；未改动 CFG 数值与游戏逻辑，可直接刷新页面查看效果。
> 读者：黎总验收画面效果 / 后续接手的开发者按本文档定位每项视觉的实现位置。
> 关联：`DESIGN.md`（玩法设计）、`index.html`（实现）、`index.backup-20260821.html`（本次改动前的完整备份，可回滚）。

---

## 1. 设计目标

| 目标 | 判断标准 |
|------|----------|
| 岛屿"浮"起来 | 岛屿有投影、背景有暗角，画面有层次而不是一张平纸 |
| 操作有回应 | 悬停己方节点有光环+手型；出兵球"弹"出来并带拖尾；占领全屏震一下 |
| 状态一眼看懂 | 顶栏领先方有 👑；兵满的建筑呼吸发光（该出兵了）；箭塔被占领挂旗 |
| 画面"活" | 孢子微粒缓慢漂移、开始界面蘑菇上下浮动（全部随 gameTime，暂停即静止） |

**红线**：不改 CFG 数值、不改战斗/AI/派兵逻辑；所有动画只读 `gameTime`（暂停时全部冻结，不产生 dt 跳变）。

---

## 2. 改动清单（实现位置以代码注释为准）

### A. 界面层（HTML/CSS）

| # | 改动 | 位置 |
|---|------|------|
| A1 | HUD 顶栏：毛玻璃（backdrop-filter blur）+ 底部软阴影 | `#hud` |
| A2 | **领先方高亮**：占比最高的一方进度条发光、百分比后带 👑（`updateHUD` 每帧计算，`maxV>0` 才亮，避免开局 0% 乱闪） | `.camp.leader` / `updateHUD()` |
| A3 | 顶栏按钮悬停上浮 + 阴影加深 | `#hud button:hover` |
| A4 | 底部提示改为居中悬浮药丸（白底毛玻璃圆角，替代原来的纯文字） | `#tip` |
| A5 | 四个遮罩（开始/选阵营/暂停/结算）统一加背景模糊 + `overlayFade` 淡入 | 各 overlay 选择器 |
| A6 | 结算卡片 `cardPop` 弹入（回弹曲线），图标 `iconBounce` 旋转弹入 | `#banner .card/.icon` |
| A7 | 开始界面：蘑菇图标缓慢上下浮动 + 顶部装饰行（🍄🌿🍄 错峰 bob 动画） | `#startScreen` |
| A8 | 阵营卡：顶部 3px 阵营色条 + 悬停时描边变阵营色（颜色由 JS `--fac-accent` 注入，改 `FS_ACCENT` 即改色） | `.fs-faction` / `buildFactionCards()` |

### B. 画布层（Canvas）

| # | 改动 | 位置 |
|---|------|------|
| B1 | 桌面背景：纵向纸面渐变（按 W/H 缓存，不每帧重建）+ 径向暗角 | `draw()` 第 0 步 |
| B2 | 岛屿投影：`shadowBlur 28 + offsetY 12`，岛屿从桌面"浮起"；上缘一道白色受光描边 | `draw()` 第 1 步 |
| B3 | **漂浮孢子**：22 颗浅色微粒按正弦轨迹缓慢漂移；位置种子锁死不闪，运动由 `gameTime` 驱动 | `makeGrain()` 生成 / `drawSpores()` |
| B4 | **占领震屏**：任意节点易主 → `shake=0.14s`，只摇世界层（ctx.save/translate），HUD 是 DOM 不受影响 | `arriveBall()` / `update()` 衰减 / `draw()` |
| B5 | **兵球拖尾**：行进中朝来向拉半透明尾巴，站桩射击时不画 | `drawBall()` |
| B6 | **出生缩放**：球从村口走出 0.18s 内从 55% 弹到 100%（`popIn` 计时） | `update()` 出发分支 / `drawBall()` |
| B7 | **满员呼吸提示**：已占领且兵力到顶的建筑画呼吸光环（alpha 随 `gameTime` 脉动） | `draw()` 第 5.5 步 |
| B8 | **悬停高亮**：指针落在己方节点 → 呼吸光环 + 手型光标（拖拽中不显示，避免与引导线打架） | `pointermove` / `draw()` 第 5.6 步 |
| B9 | **箭塔旗帜**：被占领的塔顶挂势力色三角旗，`sin(gameTime)` 摆动；中立不挂 | `drawTower()` |

---

## 3. 性能与兼容性说明

- 背景渐变按 `W|H` 缓存（`bgGrad/bgKey`），窗口不变就不重建；暗角每帧一个径向渐变，开销可忽略。
- 孢子 22 颗 + 菌点 160 颗，均为简单 `arc` 填充，远低于兵球/粒子峰值开销。
- 震屏用 `ctx.save/translate/restore` 包住世界层，粒子/拖拽线一起摇，符合直觉；幅度 `shake*22px`、0.14s，短促不晕。
- 全部新动画只读 `gameTime`：暂停时冻结，恢复无跳变（与既有 `lastTs` 重置机制兼容）。
- 不引入任何外部资源/字体/图片，单文件离线可跑的原则不变。

## 4. 自测清单（刷新页面即可验证）

- [ ] 开始界面：蘑菇图标缓慢浮动，点「开始游戏」卡片弹入
- [ ] 选阵营：卡片顶部有阵营色条，悬停描边变色
- [ ] 对局中：悬停自己的绿蘑菇 → 手型 + 光环；拖出兵 → 球弹出带拖尾
- [ ] 占领任意一块地 → 画面轻震一下；箭塔被占领后塔顶挂旗
- [ ] 顶栏：占比最高的一方进度条发光 + 👑
- [ ] 某个节点兵满（数字到容量上限）→ 建筑外圈呼吸微光
- [ ] 暂停 → 孢子/旗帜/呼吸动画全部静止；继续 → 无跳变

## 5. 回滚

本次改动前的完整文件在 `index.backup-20260821.html`。如需回滚：用 SourceTree 对比 `index.html` 的差异选择性丢弃，或直接以备份覆盖。
